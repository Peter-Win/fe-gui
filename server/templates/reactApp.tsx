import * as React from "react";
import { MainFrame } from "./MainFrame";
<%= importStyle %>
const App: React.FC = () => (
    <React.StrictMode>
        <MainFrame />
    </React.StrictMode>
);

export default App;
